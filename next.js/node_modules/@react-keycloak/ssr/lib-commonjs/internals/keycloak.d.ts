import type { AuthClient } from '@react-keycloak/core';
import type { KeycloakConfig } from 'keycloak-js';
import type { TokenPersistor } from '../persistors/types';
export declare const getKeycloakStub: (persistor: TokenPersistor) => AuthClient;
export declare const getKeycloakInstance: (keycloakConfig: KeycloakConfig, persistor?: TokenPersistor | undefined, recreate?: boolean) => AuthClient;
